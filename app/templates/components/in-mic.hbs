{{#if supported }}
    <div class="mic {{if switchedOn 'switched-on'}}">
      <a href="#"><i class="fa fa-microphone"></i></a>
      <p class="intro">Hi! I am Eva</p>
    </div>
{{else}}
  <p class="unsupported">Your browser doesn't support speech API</p>
{{/if}}
